# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Essentials
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,D,spawn,qs -c noctalia-shell ipc call launcher toggle
bind=SUPER,X,spawn,qs -c noctalia-shell ipc call sessionMenu toggle
bind=SUPER,Return,spawn,kitty
bind=SUPER,Q,killclient
bind=SUPER,Space,togglefloating
bind=SUPER+SHIFT,Space,togglemaximizescreen
bind=SUPER,grave,focuslast
bind=SUPER,S,toggle_scratchpad
bind=SUPER+SHIFT,R,reload_config
bind=SUPER+ALT,L,switch_layout
bind=SUPER,Tab,focusstack,next
bind=SUPER+SHIFT,Tab,focusstack,prev
bind=ALT,Tab,toggleoverview
bind=ALT,grave,focuslast


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Screenshot
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=CTRL,Print,spawn_shell,~/bin/hyprshot --fullscreen
bind=SUPER+SHIFT,S,spawn_shell,~/bin/hyprshot --region


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Scratchpads
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,P,setkeymode,scratchpads

keymode=scratchpads
bind=NONE,Escape,setkeymode,default

bind=NONE,C,toggle_named_scratchpad,gnome-calculator,none,gnome-calculator
bind=NONE,M,toggle_named_scratchpad,com.spotify.Client,none,spotify
bind=NONE,T,toggle_named_scratchpad,kitty-dropterm,none,kitty --class kitty-dropterm
bind=NONE,Y,toggle_named_scratchpad,explorer,none,kitty --class explorer -e yazi
bind=NONE,L,toggle_named_scratchpad,git-terminal,none,kitty --class git-terminal -e lazygit
bind=NONE,F,toggle_named_scratchpad,thunar,none,thunar

keymode=default


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# GUI Applications
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Enter guiapps keymode
bind=SUPER,G,setkeymode,guiapps

keymode=guiapps
bind=NONE,Escape,setkeymode,default

bind=NONE,B,spawn,brave-browser
bind=NONE,F,spawn,thunar
bind=NONE,E,spawn_shell,emacsclient -c -a 'emacs'

keymode=default

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# TUI Applications
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,T,setkeymode,tuiapps

keymode=tuiapps
bind=NONE,Escape,setkeymode,default

bind=NONE,Y,spawn,kitty -e yazi
bind=NONE,B,spawn,kitty -e btop
bind=NONE,E,spawn,kitty -e nvim

keymode=default


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Noctalia Launchers and Menu
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,L,setkeymode,noctalia_launchers

keymode=noctalia_launchers
bind=NONE,Escape,setkeymode,default

bind=NONE,D,spawn,qs -c noctalia-shell ipc call launcher toggle
bind=NONE,V,spawn,qs -c noctalia-shell ipc call launcher clipboard
bind=NONE,W,spawn,qs -c noctalia-shell ipc call launcher windows
bind=NONE,Period,spawn,qs -c noctalia-shell ipc call launcher command

keymode=default


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Noctalia Core Interface
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,N,setkeymode,noctalia_core

keymode=noctalia_core
bind=NONE,Escape,setkeymode,default

bind=NONE,C,spawn,qs -c noctalia-shell ipc call controlCenter toggle
bind=NONE,Comma,spawn,qs -c noctalia-shell ipc call settings toggle
bind=NONE,A,spawn,qs -c noctalia-shell ipc call calendar toggle
bind=NONE,I,spawn,qs -c noctalia-shell ipc call systemMonitor toggle

keymode=default


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Noctalia Misc
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Enter noctalia_misc keymode
bind=SUPER,M,setkeymode,noctalia_misc

keymode=noctalia_misc
bind=NONE,Escape,setkeymode,default

bind=NONE,I,spawn,qs -c noctalia-shell ipc call idleInhibitor toggle
bind=NONE,K,spawn,qs -c noctalia-shell ipc call lockScreen lock
bind=NONE,P,spawn,qs -c noctalia-shell ipc call powerProfile cycle
bind=NONE,X,spawn,qs -c noctalia-shell ipc call sessionMenu lockAndSuspend

keymode=default


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Focus
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
bind=SUPER,Up,focusdir,up
bind=SUPER,Down,focusdir,down


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Move Window
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER+SHIFT,Left,exchange_client,left
bind=SUPER+SHIFT,Right,exchange_client,right
bind=SUPER+SHIFT,Up,exchange_client,up
bind=SUPER+SHIFT,Down,exchange_client,down


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Resize Window
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER+CTRL,Left,resizewin,-30,0
bind=SUPER+CTRL,Right,resizewin,30,0
bind=SUPER+CTRL,Up,resizewin,0,-30
bind=SUPER+CTRL,Down,resizewin,0,30



# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Mouse Scrolling
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
axisbind=CTRL+SUPER,DOWN,viewtoleft
axisbind=CTRL+SUPER,UP,viewtoright
axisbind=SUPER,DOWN,viewtoright
axisbind=SUPER,UP,viewtoleft


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Move / Resize with Mouse
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER,btn_right,moveresize,curresize


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Special Workspace
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,minus,minimized
bind=SUPER,equal,restore_minimized


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Audio & Brightness
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bindl=NONE,XF86AudioRaiseVolume,spawn,qs -c noctalia-shell ipc call volume increase
bindl=NONE,XF86AudioLowerVolume,spawn,qs -c noctalia-shell ipc call volume decrease
bindl=NONE,XF86AudioMute,spawn,qs -c noctalia-shell ipc call volume muteOutput
bindl=NONE,XF86AudioMicMute,spawn,qs -c noctalia-shell ipc call volume muteInput
bindl=NONE,XF86MonBrightnessUp,spawn,qs -c noctalia-shell ipc call brightness increase
bindl=NONE,XF86MonBrightnessDown,spawn,qs -c noctalia-shell ipc call brightness decrease


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Laptop Lid
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Prerequisite: HandleLidSwitch=ignore in /etc/systemd/logind.conf (see top note)
switchbind=fold,spawn,qs -c noctalia-shell ipc call lockScreen lock
switchbind=fold,disable_monitor,eDP-1
switchbind=unfold,enable_monitor,eDP-1


# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Tags Navigation
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
bind=SUPER,1,view,1
bind=SUPER,2,view,2
bind=SUPER,3,view,3
bind=SUPER,4,view,4
bind=SUPER,5,view,5
bind=SUPER,6,view,6
bind=SUPER,7,view,7
bind=SUPER,8,view,8
bind=SUPER,9,view,9

bind=SUPER+SHIFT,1,tagsilent,1
bind=SUPER+SHIFT,2,tagsilent,2
bind=SUPER+SHIFT,3,tagsilent,3
bind=SUPER+SHIFT,4,tagsilent,4
bind=SUPER+SHIFT,5,tagsilent,5
bind=SUPER+SHIFT,6,tagsilent,6
bind=SUPER+SHIFT,7,tagsilent,7
bind=SUPER+SHIFT,8,tagsilent,8
bind=SUPER+SHIFT,9,tagsilent,9

bind=SUPER+ALT,1,tag,1
bind=SUPER+ALT,2,tag,2
bind=SUPER+ALT,3,tag,3
bind=SUPER+ALT,4,tag,4
bind=SUPER+ALT,5,tag,5
bind=SUPER+ALT,6,tag,6
bind=SUPER+ALT,7,tag,7
bind=SUPER+ALT,8,tag,8
bind=SUPER+ALT,9,tag,9

# bind=CTRL+ALT,Right,viewtoright
# bind=CTRL+ALT,Left,viewtoleft

bind=SUPER,bracketleft,viewtoleft_have_client
bind=SUPER,bracketright,viewtoright_have_client
bind=SUPER+SHIFT,bracketleft,tagtoleft
bind=SUPER+SHIFT,bracketright,tagtoright

