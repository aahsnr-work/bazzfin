# Bazzite ships the same file (system_files/desktop/shared/usr/lib/
# modules-load.d/wine-ntsync.conf): load the ntsync module at boot so
# Wine/Proton can use NT synchronization primitives (NTsync) for much
# better frame pacing in Windows games. Requires the ogc kernel, which
# builds ntsync.
ntsync
