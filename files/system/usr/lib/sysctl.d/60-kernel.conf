# Bazzite: avoids the large performance hit (up to ~30%) games take on
# Alder Lake+ CPUs when split-lock detection triggers. Trade-off: a
# misbehaving thread can slow down its core. (kernel.split_lock_mitigate=0)
kernel.split_lock_mitigate=0
